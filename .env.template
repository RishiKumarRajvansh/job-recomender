# Development environment variables - RENAME TO .env FOR USE
# This is a template file - copy to .env and modify as needed

# Environment configuration
FLASK_ENV=development  # Change to 'production' for production environment
FLASK_DEBUG=True       # Set to 'False' in production

# Security
SECRET_KEY=rishi-123#@  # Change this to a random secret key in production

# Database
DATABASE_URL=sqlite:///instance/job_recommender.db  # Default SQLite database
# For PostgreSQL in production, use something like:
# DATABASE_URL=postgresql://username:password@localhost:5432/job_recommender
